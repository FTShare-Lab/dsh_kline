# 关键点位与清除标注验收（2026-09-07）

## 交付

- 关键点位按钮发送当前已加载、已聚合的图表 K 线至本地 `analyze_key_levels`，复用既有支撑/压力算法；不再搜索证券或调用行情源，不重新加载图表。
- 保持当前周期、复权、指标和缩放。少于 5 根有效 K 线提示扩大范围；无有效点位与计算失败分别提示。
- 切换标的、改变图表、清除分析或退出对话后，晚到结果不覆盖当前图表。
- 清除标注菜单提供分析标注、手动画线、自定义点位、全部清除。全部清除需二次确认，取消不改变数据。
- 清除只针对当前对话工作台中的当前标的，保留其他标的、指标、预警。清除同时更新持久化状态，避免刷新恢复旧标注。
- 小窗口菜单挂载在工作台层，不被横向滚动工具栏裁切。

## 验收

- Python 70 项、Node 23 项通过；构建和 `git diff --check` 通过。
- 真实浏览器：上证日线 165 根，按钮仅发出 `analyze_key_levels`，显示 6 个点位；年线 5 根亦成功，不请求 FTShare。
- 创建水平线、趋势线、自定义点位；分类清除分析后画线和自定义点位仍在，清画线后自定义点位仍在。
- 全部清除取消后数量不变；确认后自动点位、手动画线、自定义点位均为零；刷新后仍为零。
- 700 × 900 小窗口可打开菜单、展开确认并取消；无页面脚本异常。
- 独立诊断浏览器测试，未清除用户原浏览器的标注。DSH 已重启加载服务端更新。

## 边界

“本地计算”依然需要本机 DSH 服务在线，但不需要行情源在线；首次加载行情仍依赖数据源。算法沿用最近最多 60 根的分形聚类规则，并非所有行情都会产生有效点位。
