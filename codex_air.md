# dsh_kline · Codex Air 工作日志

更新日期：2026-09-09  
维护规则：从本次同步开始，**本设备后续的设计、实现、测试、验收与发布准备统一记录在本文件**。`codex_pro.md` 保留为另一台设备完成的安装、发布与 Windows 兼容性实施记录；除非明确需要修订其事实，不再在其中追加本机工作。

## 同步基线

- 已同步远端主线：`origin/main` 的 `ae9f22d`（Windows 原生支持合并后的主线）。
- 当前包版本、远端 tag 与 GitHub Latest Release 均为 `v0.2.2`，并指向 `ae9f22d`。该版本已于 2026-09-09 正式发布，Release 标题为“Windows 原生安装支持”。
- 远端主线已包含：跨平台 Node 启动器、Windows Python/路径/凭据适配、Windows CI、发布包隔离启动测试，以及对应的 `codex_pro.md` 验收记录。
- 本机没有提交、推送、打标签或创建 Release 的授权；所有发布动作必须等用户明确许可。

## 本机待验收工作（未发布）

以下改动已在本机工作区保留，需在远端 `0.2.2` 基线上重新构建和回归后才可进入发布讨论：

1. **板块与市场情报**
   - 新增“板块”一级页面，聚合市场涨跌分布、热门板块、板块归属、资金流与交易事件。
   - 新增独立 MCP / 图表服务动作：`market_pulse`、`security_intelligence`。
   - 页面必须对“无数据、权限不足、限流、标的不适用”给出直观说明，不能把空数据伪装成行情结论。
   - 2026-09-09 已定位并修复首轮真实数据适配问题：旧实现误取大盘接口的最早记录、把地区板块当作热门行业，并漏读 `main_net` / `main_pct` 字段，导致页面出现 `--`、过期日期和无意义的地区列表。现在改为取最新市场记录、按主力资金排序的行业板块，并正确展示个股主力净流入及占比；行业归属没有数值时不再显示伪造的 `--`。

2. **ETF 支持**
   - 补充常用 ETF 的搜索种子。
   - 日线与分钟线走 FTShare ETF 专用通道，不与个股通道混用。
   - ETF 在“板块/公司资料”等不适用页面必须明确说明，而不是展示失真的公司数据。

3. **跨对话自选**
   - 自选从“当前对话的浏览器状态”迁移为用户级持久化状态。
   - 图表、对话与不同工作台入口读取同一份自选；仍需保持用户标注、当前图表和对话分析的会话隔离。

4. **信息架构优化**
   - 顶部一级页面收敛为：`图表 · 板块 · 资讯 · 公司`，避免二级 Tab 与层层菜单。
   - “公司”页以连续信息块直接展示公司画像、财务表现、股东与筹码；保留旧工作区 `简况 / 财务 / 股东` 偏好的兼容迁移。
   - “板块”页以市场脉搏、行业热度、所属行业、同类股、相关 ETF、个股资金与可用事件构成连续信息流；行业资金和个股资金采用相对强弱条，不再只是两列原始记录。
   - 相关 ETF 先提供行业 ETF（有明确行业映射时）与宽基市场参考 ETF；新增银行、证券、半导体、医药、酒与新能源车 ETF 的本地搜索种子。
   - 窄侧栏允许横向滚动一级导航，避免压缩或截断页面名称。

## 下一步：为下一版本完成可信验收

优先级按风险而不是按功能数量排序：

1. **合并态回归**：重新生成客户端产物，运行 Python、Node、打包与隔离安装测试，确认 Windows 启动器与本机业务改动没有相互回退。
2. **数据正确性**：验证 A 股、ETF、指数、港股的日线/可用分钟线；验证板块页在有数据和无数据时的提示，不对受限数据做推断。
3. **持久化与隔离**：至少验证两个对话间自选一致、图表/标注不串、关闭再打开仍可读取自选。
4. **产品界面验收**：验证四个一级页面、窄宽度、空数据、加载失败和刷新行为；重点检查用户能否理解“数据源没有提供”而非误以为插件崩溃。
5. **Windows 真实环境验收**：代码和 CI 已覆盖，但在真实 Windows DSH Web 上完成市场安装、首次启动、配置 Key、看图、升级和重启前，不对外宣称已完成 Windows 人工验收。

### 本机合并态验证记录（2026-09-09）

- 已将本机工作安全叠加到 `origin/main` 的 `ae9f22d` 基线；远端 Windows 启动与 CI 改动没有被覆盖。
- Python：`106 passed, 1 skipped`。跳过项只适用于真实 Windows 环境。
- Node / 前端：`57 passed`；TypeScript 与侧栏打包构建通过。
- ~~下一版本的发布阻塞项~~：标准 `pnpm pack` 曾把测试运行生成的 `core/**/__pycache__/*.pyc` 与 `tools/**/__pycache__/*.pyc` 一并收入 tarball。已在 `package.json` 明确排除、发布打包器同步过滤，并用真实 `pnpm pack` 与 `build-release-package` 复核：当前 tarball 为 56 个文件，不含任何 Python 缓存。
- 隔离安装冒烟测试尚未作为本次合并态的最终结论记录，必须在上述 tarball 卫生问题修复后重新运行。

### 板块页修复验证（2026-09-09）

- 真实 FTShare 返回验证：涨跌停、最新上证指数资金记录、行业主力资金排行和贵州茅台的主力净流入均得到有效值；不再展示旧实现的 `--`、年初市场记录或地区板块前缀。
- 自动化验证：市场情报 Python 测试 5 项、公司工作台前端测试 6 项、侧栏构建均通过。
- 本地 DSH Web 已重启；已打开的浏览器页面需要刷新一次才会载入新的客户端 bundle。

### 四页信息架构与板块可视化验证（2026-09-09）

- 已移除一级“简况 / 财务 / 股东”，改为不嵌套的“公司”页；旧会话保存的三个入口会自动落到公司页，避免升级后出现空页。
- “新闻”重命名为“资讯”，不再在 Tab 标题显示数量。
- 板块页新增市场指标卡、按主力资金相对强弱显示的行业与个股资金条，以及可直接打开行情的相关 ETF 卡；数值仍同时保留原始文字与正负号，不能只依赖颜色表达。
- 自动化回归：Python `107 passed, 1 skipped`；Node `55 passed`；侧栏构建与产物校验通过；`git diff --check` 通过。未执行提交、推送、tag 或 Release。

### 用户级自选持久化修复（2026-09-09）

- 自选的正确归属是**用户级常态化数据**：服务端保存至 `~/.cache/dsh_kline/watchlist-v1.json`，浏览器 localStorage 仅作为离线回退与迁移来源；它不再依赖任一对话、图表 Tab 或浏览器页面内存。
- 已验证本地运行中的图表服务已加载 `watchlist_get` 动作，实际响应为成功且返回共享 revision；此前“unsupported_chart_action”来自尚未重启的旧进程，而非端点设计缺失。
- 服务端写入现在采用 revision 乐观并发控制：过期客户端的完整写入会返回 `watchlist_conflict`，不会覆盖另一对话刚保存的数据。时间戳统一毫秒级，兼容早期秒级存档。
- 客户端保存携带 revision 与更新时间；冲突时合并两端新增的分组/标的并重试，保存失败仍保留本地离线副本，下一次侧栏打开或初始化会再次同步。
- 复核：Python `108 passed, 1 skipped`；Node `56 passed`；侧栏构建、真实打包和发布包缓存检查通过；本地 DSH Web 已在新代码后重启。未执行提交、推送、tag 或 Release。
- 用户已在真实界面完成跨对话自选验证，结果成功；该项人工验收已通过。
- 已准备本地 `v0.2.3` 候选：版本号、用户向更新记录与 README 已同步；构建通过，Python `108 passed, 1 skipped`、Node `56 passed`，npm 与 Release 安装包各 56 个文件且不含 Python 缓存。仍未执行提交、推送、tag 或 Release。
- 本轮新增关键点位参考卡：基于已完成的关键点位分析展示支撑、压力、突破、风险与目标五类研究参考，并可一键保存为个人图表点位；自选侧栏的最新价、日涨跌幅、分组概览与涨跌排序补充了自动化覆盖。回归为 Python `108 passed, 1 skipped`、Node `58 passed`，构建与 diff 检查通过；仍未执行 Git 写操作。
- 修复关键点位分析成功后通用完成浮层遮挡顶部实时涨跌摘要：成功时由参考卡承担完成反馈，不再显示重复浮层；同时从客户端移除了“关键点位分析已更新”这条成功文案，避免旧完成提示被任何路径重新调用。无结果或失败仍保留明确反馈。已重建并重启本地 DSH Web。
- 关键点位交互进一步收敛：分析只生成候选与五张参考卡，不会一次性在图上绘制所有支撑、压力候选；用户选择“保存为我的点位”后，才显示该条个人水平线。新增候选默认隐藏的自动化测试；关键点位相关 Node 回归 `10 passed`、构建与 diff 检查通过。
- 修复报价区浮层遮挡的通用根因：此前“资讯与简况已更新”等任意成功提示复用固定在报价行的全局 notice。现在加载与成功反馈只留在对应页面内容中；全局 notice 仅用于失败提示，且固定在底部、不会覆盖报价或一级导航。Node 相关回归 `11 passed`，构建与 diff 检查通过。
- 修复独立工作台的搜索/交互请求路由：本地证券目录与服务端搜索均正常（`紫金矿业 → 601899.XSHG` 已直连验证），但前端误将交互请求发往 DSH 宿主的 `/api/tools`，而非插件的 `/dsh-kline/api/tools` 受控代理，导致新工作台里名称搜索无候选。已统一修正入口并增加路由回归测试；Node 相关回归 `12 passed`，构建与 diff 检查通过。

## 下一阶段：Market Intelligence Core 的边界

在当前版本稳定后，再将产品整理为一个统一的 **Market Intelligence Core**，并让 MCP、Agent、图表和用户工作台只是独立客户端。

建议先固化五个可独立调用的领域能力：

- **Instrument Directory**：代码、名称、市场、ETF/指数/股票分类与搜索。
- **Market Data**：行情快照、OHLCV、交易日、复权与数据源能力说明。
- **Research & Intelligence**：新闻、公司资料、财务、股东、板块、资金流、事件。
- **Portfolio State**：用户级自选与分组；不与任一对话绑定。
- **Analysis**：指标、区间统计、关键点位、预警与图表可消费的标准结果。

这样 MCP、Agent、图表和工作台不会各自复制业务逻辑：它们只负责调用核心能力并呈现结果。第一步不应是大规模重写，而是为现有入口定义稳定的数据模型和能力边界，再逐项迁移。

## 发布纪律

- 未经用户明确许可，不执行提交、推送、tag 或 GitHub Release。
- 发布前必须记录：最终版本号、变更范围、自动化结果、关键人工验收与已知限制。
- 不把“接口暂未返回数据”写成“功能可用”；不把 CI 通过写成“真实 Windows 用户已验收”。

### 市场与板块职责拆分（2026-09-09）

- 一级导航调整为：`市场 · 图表 · 板块 · 资讯 · 公司`。新增的“市场”排在图表之前，承接全市场数据；不会增加二级页面。
- “市场”只消费独立的 `fetch_market_pulse`：市场宽度、涨跌停/大盘资金指标与热门行业排行。首次打开时按需加载，顶部刷新仅在该页打开时刷新它，避免用户切换标的时重复请求相同全市场数据。
- “板块”只消费当前标的的 `fetch_security_intelligence`：所属行业、行业资金、同类股、关联 ETF、个股资金与交易事件。已移除其中原有的市场脉搏与热门行业区块，不再混合全市场和个股语义。
- 对话/标的工作区仍各自记住当前页签；市场数据本身是全局只读快照，不会因切换标的被清空。
- 定向前端回归：21 项通过；`pnpm build:sidebar` 与 `git diff --check` 通过。未执行提交、推送、tag 或 Release。

### 市场首页与板块空态优化（2026-09-09）

- 市场页升级为跨市场概览：按**中国内地 / 香港 / 美国**分组展示可用核心指数卡，可直接点击打开相应 K 线；其后才展示 A 股市场宽度、大盘资金与行业热度，避免把“市场情绪”误当成某只股票的板块信息。
- 已对当前 FTShare 实际请求验证 8 个指数全部可用：上证指数、沪深 300、深证成指、创业板指、恒生指数、纳斯达克 100、标普 500、道琼斯。全局指数仍严格使用官方 FTShare 路径；单个区域临时缺失不会导致其余官方指数整体降级。
- 招商银行的实测结果为：行业“银行”、行业资金记录及 10 条个股资金记录均正常；“同类股”为空是上游同类公司接口未对该一级行业返回名单。前端已把这类**局部字段为空**改为准确提示，不再写成“板块、资金或事件都没有数据”。
- 新增市场指数覆盖与市场页交互回归；定向 Python 6 项、前端 10 项均通过。未执行提交、推送、tag 或 Release。

### 市场页视觉收敛（2026-09-09）

- 移除“主要指数”下的说明性文案，以及市场概览、行业热度下重复解释；页面保留标题和数据本身。
- 三地指数由同高表格容器改为按内容自然高度排列的紧凑卡片流；香港只有一项指数时不再出现大面积空白。
- 指数卡保留名称、代码、现价与涨跌幅，交互和可点击打开 K 线能力不变；视觉重点从边框和说明转回行情数值。

### 市场页行情卡重构（2026-09-09）

- 参考主流券商“Markets / 全球指数”信息层级，放弃地区纵向表格。首页首屏改为自适应的独立行情卡网格：地区仅以小标签标示，指数名称、现价和涨跌幅为视觉主次。
- 未加入来源无法验证的世界地图、分时缩略图、期货/商品/外汇卡；当前 FTShare 请求只展示已实测可用的 8 个大盘指数，不伪造额外行情。
- 窄工作台固定双列，高宽度自动扩展；单张卡均可打开对应 K 线。定向 Node 10 项、构建和包校验通过；DSH Web 已重启，未执行 Git 写操作。

### 市场页密度与色彩修订（2026-09-09）

- 移除卡片内的“中国内地 / 香港 / 美国”文字标签；市场归属不再占据首屏阅读空间。
- 行情卡收敛为两列信息结构：左侧名称与代码，右侧现价与涨跌幅；上涨、下跌仅通过细侧边与涨跌数字作克制提示。
- 统一卡片高度、圆角和留白，市场概览改为三项等宽指标；无行业热度数据时不显示空白区块。
- 定向前端测试、构建与包校验通过；DSH Web 已重启使该版本生效。未执行 Git 写操作。

### 市场情报扩展：概念与股票排行（2026-09-09）

- `fetch_market_pulse` 新增两类独立、可降级的数据：按主力资金排序的**概念**，以及东方财富 A 股**人气／飙升**排行榜。行业、概念和股票榜不再混用同一个字段或标题。
- 市场页不增加页面层级；“热门方向”内用紧凑切换查看行业或概念，“股票排行”内用紧凑切换查看人气或飙升。排行卡带规范化代码，点击可直接打开对应 K 线。
- 各区块仅在上游真实返回内容时呈现。接口限流、无权限、字段为空或其他数据源未实现时，会保留指数与已有市场信息，同时隐藏该区块，不制造空白榜单或错误提示。
- 新增后端概念/排行隔离与代码规范化测试；市场前端定向测试、构建、包校验和 diff 检查通过。未执行提交、推送、tag 或 Release。

### 市场排行兜底与龙虎榜（2026-09-09）

- 根据 FTShare 2026-09-07 的套餐表复核：东方财富股票排名、雪球股票排名、龙虎榜总览与龙虎榜明细均属于免费版接口；不需要专业套餐。
- 东方财富人气／飙升榜在短时间内可能限流。人气榜现已以雪球“关注总榜”作语义等价兜底；飙升榜只使用东方财富真实返回，缺失时不展示该切换项，避免错误替代。
- 市场页新增“龙虎榜”模块，展示最近有效快照的标的、收盘价、涨跌幅与成交额，点击可直接打开 K 线。先请求当日数据；若盘中尚无当日归档，自动回退最近有效快照。
- 实测当前免费路径：人气榜 8 条、飙升榜 8 条、龙虎榜 8 条均可取得。后端 8 项、前端 11 项、构建、包校验与 diff 检查通过。未执行 Git 写操作。

### 情报条目卡片化（2026-09-09）

- 将原先会随工作台无限拉长的全宽度排行条，重构为通用的双列信息卡。每张卡保留排名、名称、数值／涨跌与短强度条，避免中段出现无意义大面积留白。
- 该布局复用于市场热门方向、所属行业资金、个股资金等所有情报排行区域；窄于移动阅读宽度时自动退回单列。
- 前端定向 11 项、构建、包校验与 diff 检查通过。未执行 Git 写操作。

### 市场数值视觉语义统一（2026-09-09）

- 统一指数卡、股票排行卡、龙虎榜卡、行业资金卡、个股资金卡与市场概览的方向判断，不再依赖服务端百分比是否带 `+` 字符。
- 上涨／正向资金统一使用上涨色，下跌／净流出统一使用下跌色；价格数值与百分比同步上色。“涨停”固定为上涨色，“跌停”固定为下跌色。
- 后端市场宽度方向字段、前端方向规则与视觉回归均已覆盖；后端 8 项、前端 12 项、构建、包校验与 diff 检查通过。未执行 Git 写操作。

### 板块与资金数据语义清理（进行中，2026-09-09）

- 已确认 FTShare 的行业资金与个股资金接口返回的是按日期排列的历史序列；此前前端把恒定的行业／股票名称当作每张卡的标题，才造成“银行”“招商银行”重复出现。
- 正在将这些条目改为日期卡：主值为主力净额，方向色由净流入／净流出决定；行业卡补充当日领涨股，个股卡补充收盘价与涨跌。这样不再把不同维度混成无意义的重复条目。

### 数据源能力契约（进行中，2026-09-09）

- 新增一份面向所有客户端的能力契约：行情、市场指数、市场宽度、排行、板块、公司、新闻、财务、股东、资金和交易事件均分别声明状态。
- FTShare 是能力提供者；用户传入的外部 OHLCV 只声明 K 线能力，不会因“有行情”而错误显示市场、板块或公司资料页面。后续接入任何新数据源只需填同一份契约，不改 UI 的业务判断。

### 板块详情能力（进行中，2026-09-09）

- `market_pulse` 的热门行业／概念现保留 FTShare 返回的板块代码；新增独立 MCP 能力 `market_board_detail`，可按该代码读取板块快照及可用的资金历史，不依赖某张图表或某个对话。
- 实测发现同一供应商的“资金流板块代码”与“板块成分股”接口并不通用，因此成分股状态明确标为 `source_dependent`，不会伪造为空名单或错误匹配。行业资金历史和领涨标的可正常返回。

### 待办：多宿主 Adapter（2026-09-09）

- 目标是一个仓库、一套 Market Intelligence Core 与标准 MCP 工具，分别提供 DeepSeek Harness、Codex、Claude、Kimi 等宿主的薄适配层。
- 非 Sidebar 模式将演进为独立本地 Web Workbench；Sidebar 只是 DSH 的可选增强，而不是唯一入口。
- 安装器可以检测已安装宿主并生成安装计划，但不得静默修改任何宿主配置；必须由用户确认并支持显式 `--host` 覆盖。各宿主的 MCP 配置与 UI 能力须分别实机验收后才标为支持。

### v0.2.3 发布前准备（2026-09-09）

- 已补正 CHANGELOG：一级页面名称为“市场、图表、板块、资讯、公司”，并覆盖市场首页、板块详情、数据源能力契约及资金历史重复渲染修复。
- 已重新构建客户端与发布包；包校验、发布包隔离安装测试（13 通过、1 平台跳过）和 diff 检查均通过。发布包为 `release/dsh-kline.tgz`。
- 未执行 Git 提交、推送、tag 或 GitHub Release；等待用户明确授权。

### 本机测试服务（2026-09-09）

- 已为本机配置登录后常驻的 `com.ftshare.dsh-kline-web` LaunchAgent：在仓库目录启动 DSH Web，异常退出后自动拉起，日志写入临时日志文件。该配置不属于仓库，也不包含在发布包中。
- 本机 DSH profile 已改为从 v0.2.3 GitHub Release 安装真实 tarball，不再使用开发仓库软链接。该 Mac 的系统 Python 为 3.9，常驻测试服务显式使用仓库现有 Python 3.13；这是本机测试前置条件，不影响发布包。

### 龙虎榜数据与布局修复（2026-09-09）

- 龙虎榜改用 FTShare 龙虎榜明细，按五席净买入／净卖出排序；严格过滤正负净额，实际不足十条时显示真实数量，不重复凑数。
- 明细缺少中文名称时，依次使用龙虎榜总览、本地标的目录和精确代码搜索补齐；无法解析时保留代码标识，不伪造名称。
- 修正龙虎榜双栏内再次分成双列导致的窄卡片问题，现在买入、卖出两侧各自单列，名称、价格、涨跌和净额可读。
- 已完成后端市场情报、前端工作台、侧栏构建、发布包校验与差异检查；本地 DSH Web 已重启验证。
- 本轮只更新日志与工作区代码，未执行 Git 提交、推送、tag 或 Release。

### v0.2.4 候选整理与验证（2026-09-09）

- 远端 `main` 与 Latest Release 已推进到 `v0.2.3`。该版本的 Windows／Ubuntu 与 Release CI 失败原因是安装冒烟脚本仍写死 9 个 MCP 工具，而当前服务已提供 14 个；日志显示全新 venv、依赖安装和 MCP 首次启动本身成功。`v0.2.4` 改为逐项校验当前 14 个必要工具，并允许未来增加工具。
- 从本机未提交区只提取有日志依据的产品改动：新会话市场落地页、旧会话图表隔离、市场情报渐进加载、独立龙虎榜与五席净额、卡片和点击交互修订。未纳入 `pnpm-lock.yaml`、旧文档和旧测试文件等未说明删除，也未纳入本地 `release/` 产物。
- 候选版本更新为 `0.2.4`。干净工作区回归结果：Python `116 passed, 1 skipped`；Node／前端 `71 passed`；TypeScript 与侧栏构建、发布文件白名单、`git diff --check` 均通过。
- 真实发布包验收通过：从 `dsh-kline.tgz` 在中文／空格路径创建全新 venv，完成 MCP 初始化与 14 个工具发现、匿名指数行情、宿主级 locator、HTTP 健康检查和未鉴权拒绝；随后在隔离 DSH Web profile 中完成安装、bundle patch、MCP 与图表服务启动。
- 当前仍处于候选分支。只有远端 PR 的 Windows／Ubuntu 原生 CI 通过后，才合并 `main`、创建 `v0.2.4` Release，并确认固定安装包与 SHA-256 资产齐全。

### 依赖指纹与引导解释器修复（2026-09-10）

- 运行时准备完成后增加指纹落盘校验和明确日志；如果指纹无法保存，启动器会报错而不会误报“已准备”，避免后续每次重复安装依赖。
- 项目已有 `.venv` 时，将其 Python 放在引导解释器候选首位；Python 3.9 仅在没有可用项目环境时才会参与候选，不再覆盖一个可用的项目环境。
- 验证：Node 启动器路径测试 4 项通过；`git diff --check` 通过。

### 板块排行稳定性修复（2026-09-10）

- 板块与概念排行在统一渲染前按板块代码去重；供应商重复返回的 `PCB`、`新能源车` 等不会再生成重复卡片。
- 保留真实板块代码、类型和资金数据；没有数据时继续显示稳定空态，不用模拟行情填充。
- 验证：市场情报 Python 13 项通过；侧栏构建与发布包校验通过；`git diff --check` 通过。
