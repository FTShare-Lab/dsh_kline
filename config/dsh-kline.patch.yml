# The MCP service is now part of the package's root cordis.patch.yml so GitHub
# and marketplace installs mount it automatically. This file remains as a
# harmless compatibility placeholder for old launch commands.
[]
